spring.application.name=news

# Server Configuration (Render uses PORT environment variable)
# Use PORT env var if available, otherwise default to 8080
server.port=${PORT:8080}
# Ensure the application binds to 0.0.0.0 to accept external connections
server.address=0.0.0.0

# Firebase Configuration (for local development)
firebase.service-account.resource=classpath:serviceAccountKey.json

# Cloudinary Configuration (for local development - override with env vars in production)
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dhfm3zbbg}
cloudinary.api-key=${CLOUDINARY_API_KEY:722275582294986}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:55zq-WkMPSrCS-mNTYB1Xq5KDD0}

# File upload configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

